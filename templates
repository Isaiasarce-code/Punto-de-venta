<!doctype html>
<html>
<head><title>Punto de Venta</title></head>
<body>
  <h2>Buscar producto por código de barras</h2>
  <form method="post">
    <input name="codigo" placeholder="Código de barras" required>
    <button type="submit">Buscar</button>
  </form>

  {% if error %}
    <p style="color:red;">{{ error }}</p>
  {% endif %}

  {% if producto %}
    <h3>Producto encontrado:</h3>
    <p><strong>Descripción:</strong> {{ producto.descripcion }}</p>
    <p><strong>Precio:</strong> ${{ producto.precio }}</p>
    <p><strong>En stock:</strong> {{ producto.cantidad }}</p>

    <form method="post" action="/vender">
      <input type="hidden" name="codigo" value="{{ producto.codigo }}">
      <input type="number" name="cantidad" min="1" max="{{ producto.cantidad }}" required placeholder="Cantidad a vender">
      <button type="submit">Vender</button>
    </form>
  {% endif %}
</body>
</html>
